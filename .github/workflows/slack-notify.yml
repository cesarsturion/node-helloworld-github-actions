name: Notify on Release
on:
  workflow_dispatch:
  push:
    branches:
      - 'feature/*'
      - '!master'
      - '!main'
  pull_request:
    branches: 
      - 'master'
      - 'main'
  release:
    types:
      - 'created'

jobs:
  slack-message:
    runs-on: ubuntu-latest
    steps: 

      - name: Text Message
        run: |
          curl -X POST -H 'Content-type: application/json' --data '{"text":"Hello, World! >>> ${{ github.run_id }} <<< "}' https://hooks.slack.com/services/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
